export type Sale = {
    //nome da variavel lá no backend e dps dos : informar o typo dela
    //Famosa TIPAGEM
    id: number,
    sellerName: string,
    date: string,
    visited: number,
    deals: number,
    amount: number
}

